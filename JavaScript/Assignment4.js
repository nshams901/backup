/*
Create a function that takes a number (step) as an argument and returns the number of matchsticks in that step.
*/


const matchHouse = (input) => {
    let matchSticks = 5 * input + 1;
    console.log(matchSticks);
}

matchHouse( 4 );